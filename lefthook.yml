# lefthook.yml

pre-commit:
  parallel: true
  commands:
    markdown-lint:
      glob: "docs/*.{md}"
      run: ./website/node_modules/.bin/markdownlint {staged_files}
    readme-lint:
      glob: "README.md"
      run: npx markdownlint {staged_files}